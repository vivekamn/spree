<%= render :partial => 'admin/shared/configuration_menu' %>

<h1><%= t("new_locale") %></h1>
<%= error_messages_for :locale %>

<% form_for(:locale, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t("create") %>
  </p>
<% end %>